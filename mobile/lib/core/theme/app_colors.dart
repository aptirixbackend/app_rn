import 'package:flutter/material.dart';

/// HOMELY brand palette (derived from the UI design).
class AppColors {
  static const primary = Color(0xFF5B4EE8); // indigo/violet
  static const primaryDark = Color(0xFF4A3FD0);
  static const primarySoft = Color(0xFFEEEDFF); // light chip / icon background

  static const ink = Color(0xFF1B1D28); // headings / primary text
  static const inkSoft = Color(0xFF6B7280); // secondary text
  static const border = Color(0xFFE6E7EE); // input borders / dividers
  static const boxBorder = Color(0xFFE2E3EA); // OTP box border

  // Goal / role selection
  static const green = Color(0xFF23A45D); // "Search a Home" accent
  static const postCard = Color(0xFFEFECFF); // lavender card background
  static const searchCard = Color(0xFFE8F6EF); // green card background

  // "What are you looking for?" preference chips
  static const prefGreen = Color(0xFF23A45D);
  static const prefGreenBg = Color(0xFFE7F6EE);
  static const prefPurple = Color(0xFF7A5AF8);
  static const prefPurpleBg = Color(0xFFEEEAFE);
  static const prefOrange = Color(0xFFF08C3E);
  static const prefOrangeBg = Color(0xFFFCEFE1);
  static const prefBlue = Color(0xFF3B82F6);
  static const prefBlueBg = Color(0xFFE6F0FE);
  static const prefPink = Color(0xFFEC5B8C);
  static const prefPinkBg = Color(0xFFFBE7F0);

  static const surface = Color(0xFFFFFFFF);
  static const scaffold = Color(0xFFFFFFFF);
}
